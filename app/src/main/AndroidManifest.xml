<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.i053114.myapplication">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Views.RegisterItemsActivity" />
        <activity android:name=".Views.ListActivity" />
        <activity android:name=".Views.DescriptionActivity" />
        <activity android:name=".Views.IngresarActivity" />
        <activity android:name=".Views.NewCommentActivity" />
        <activity android:name=".Views.ListCommentActivity" />
        <activity android:name=".Views.ListaFavoritosActivity"></activity>
    </application>

</manifest>